# IELTS 项目协作说明

## 项目定位

这是一个本地使用的 IELTS 学习资料库。正式功能只分为两个静态站点：

- `雅思口语练习/`
- `雅思批改作文/`

调研文档统一放在 `调研资料/`，该目录仅供本地参考，已由根目录 `.gitignore` 排除，不要提交或推送。

## 已确定的技术方案

- 保持零框架：只使用原生 HTML、CSS 和 JavaScript。
- 不增加 Node.js、包管理器、构建步骤、本地服务器或前端框架。
- 用户直接双击各站点的 `index.html` 使用；不要恢复 `.command` 启动脚本。
- 页面素材保存在各自的 `data.js`，通过 `window.IELTS_DATA` 等全局变量由普通 `<script>` 标签载入，不使用 `fetch`。
- 使用浏览器本地存储记住当前页面、所选 Tab 和各阅读视图的滚动锚点；刷新后应恢复上次阅读位置。
- 口语素材不拆成 JSON 或 JSONL。写作站的 JSONL 导入/导出是用户作文备份功能，并非站点素材存储，必须保留。
- 每个站点的运行文件应尽量维持为 `index.html`、`styles.css`、`app.js`、`data.js` 和已被页面引用的 `favicon.svg`。

## 口语站内容规则

- 只能使用现有题库中的原题，保留题目原文、主题和顺序；不要创造虚拟分类或额外问题。
- Part 2 页面中，“素材母版”应放在各题目列表之前。
- Part 3 每道题都要有英文答案、题目翻译和答案翻译。
- 答案面向 5.5 分目标，采用容易说出口的 Band 6 左右表达：直接、自然、句子完整，不堆砌生僻词。
- Part 3 优先采用 `观点 → 原因 → 细节/例子 → 结果或限定`，并自然调用 M1–M9 万能素材。
- 高频对比不是强制项。只有逻辑自然时才使用：C1 老人/年轻人、C2 内向/外向、C3 城市/乡村；禁止为了贴标签而硬套、答偏题。
- 标注了 C 或 M 素材时，答案正文必须真正体现相应逻辑，中文翻译也要同步更新。

## 写作站数据规则

- 内置作文、模板、范文和素材保存在 `雅思批改作文/data.js`。
- 用户编辑结果保存在浏览器本地存储中。
- JSONL 导入/导出用于备份和恢复用户批改记录，不要误删。

## 修改与验证

- 优先保持项目简单；没有明确收益时不要增加文件、抽象层或工具链。
- 修改素材时，同时核对英文、中文翻译、素材标签和页面显示。
- 修改 JavaScript 后至少运行 `node --check`；提交前运行 `git diff --check`。
- 只在用户明确要求时提交或推送 Git；调研资料始终不进入 Git。

## 历史决策摘要

项目已从较复杂的应用重构为可直接打开的静态站点；素材存储由外部数据请求改为 `data.js`；启动脚本和重复配置已删除；Part 2 素材母版已前置；Part 3 已恢复为仅包含原题库题目，并按“自然使用对比框架 + 5.5 分表达 + 万能素材”的规则维护答案。
