# 雅思批改作文

这是一个只在本机运行的雅思作文批改页面。

## 使用方法

在 macOS 上双击：

`启动雅思批改作文.command`

页面会自动在浏览器中打开。终端窗口需要保持开启；关闭终端窗口后，本地页面会停止运行。

## 数据保存

- 历史作文、删除结果保存在当前浏览器的本机存储中。
- 页面不会把作文内容发送到在线数据库。
- 清除浏览器网站数据会同时清除本地历史记录。

## 开发命令

```bash
pnpm install
pnpm dev
pnpm build
```
